# Шаблон переменных окружения (при необходимости).
# Скопируйте в .env и заполните значения. Файл .env не коммитится.

# Пример (не используется скриптами по умолчанию — пути задаются в config.json):
# INPUT_XLSX=
# OUTPUT_XLSX=
