# Roadmap: сопоставление холдингов с условными ГСЗ

## Статус проекта

| Этап | Описание | Статус |
|------|----------|--------|
| 1 | Сбор и уточнение бизнес-требований | `[v]` |
| 2 | Согласование логики AND/OR и границ слов | `[v]` |
| 3 | Проектирование шагов M-запроса | `[v]` |
| 4 | Реализация функций сопоставления (PQ) | `[v]` |
| 5 | Сборка запроса `_HOLD_OD_GSZ` | `[v]` |
| 6 | Адаптация под Excel 2016 (ошибки PQ) | `[v]` |
| 7 | Генератор ключей из Excel `_base_gsz` (только токены имени + таймштамп) | `[v]` |
| 8 | Документация (README, ROADMAP, БТ) | `[v]` |
| 9 | Ручная проверка на рабочей книге | `[ ]` |
| 10 | Доработка дубликатов ключей (ручной разбор) | `[ ]` |
| 11 | Ускоренная версия M-запроса для больших объемов | `[v]` |
| 12 | Python-реализация с параллелизацией | `[v]` |
| 13 | Оптимизация Python-матчера + регресс-тест parity | `[v]` |
| 14 | Тестовая книга `input/workbook.xlsx` (26k холдингов) | `[v]` |
| 15 | Обратная проекция в `_base_gsz`: найденные холдинги с ID и отладка | `[v]` |
| 16 | Конфигурирование добавляемых выходных колонок по ключам типов данных | `[v]` |
| 17 | Группы исключающих ключей `and_non` / `or_non` в Python-матчере | `[v]` |
| 18 | Power Query V2: `non`-ключи и универсальное число колонок | `[v]` |
| 19 | Ключ `key_fix_id`: фиксированное соответствие ГСЗ ↔ холдинг + колонка «статус» | `[v]` |
| 20 | Предфильтр: все AND/OR-токены (без отсечения до одного якоря) | `[v]` |
| 21 | Разбор `_base_kg`: уникальные ИНН из «Компании группы (ЕРЗ)» | `[v]` |
| 22 | `_base_kg` ИНН: freeze=1, ширины, кол-во регионов и имя без города | `[v]` |
| 23 | ERZ DevTools scraper: регионы → группы → компании → JSON | `[v]` |
| 24 | ERZ JSON → Excel: группы + уникальные компании по ИНН | `[v]` |

Подробное ТЗ: [ToDo/key_fix_id.md](ToDo/key_fix_id.md).

### Этап 22 — доп. колонки и формат `_BASE_KG_INN`

**Изменения:**
- `freeze_cols`: 3 → **1** (закрепление только колонки ИНН);
- ширины: ИНН 30, Наименование (компания) 70, «Наименование, регион» 100;
- колонка **кол-ва уникальных регионов** (ширина 10, центр H+V);
- колонка **наименований без города** (часть «Наименование, регион» до запятой), ширина 90;
- колонка **кол-ва** таких урезанных названий (ширина 10, центр H+V).

**Шаги:**
1. `[v]` config.json + Python агрегация/форматирование
2. `[v]` Power Query `_BASE_KG_INN.pq`
3. `[v]` тесты и README

### Этап 23 — ERZ DevTools scraper

**Цель:** собрать с erzrf.ru регионы, группы бренда и компании (группы + бренд) в JSON
через скрипт в Console DevTools (куки текущей сессии, без внешних зависимостей).

**Артефакт:** [`src/erz/erz_devtools_scraper.js`](src/erz/erz_devtools_scraper.js)

**Параметры по умолчанию:** пауза 400 мс, `costType=1`, names = один регион на группу,
`join` = **все регионы группы** (API join фильтрует по region), авто «скачать всё»,
повторы при 504 (до 3), итог `ERZ_Full_<timestamp>.json`.

**Шаги:**
1. `[v]` IIFE + модалка + `sleep` / `fetchJson` / `downloadJson` / лог
2. `[v]` dictionary → выбор регионов → `brand_count` + `brand/join` → дедуп групп
3. `[v]` `developer/join` + `developer/names` (one/all) → дедуп компаний
4. `[v]` финальный/промежуточный JSON + README/ROADMAP
5. `[v]` авто-прогон всех регионов/групп; retry 504; join по всем регионам группы

### Этап 24 — ERZ JSON → Excel

**Цель:** из `ERZ_Full_*.json` (или тестового `input/erz_full_sample.json`) собрать Excel
с двумя листами: группы застройщиков и уникальные компании по ИНН.

**Артефакты:**
- Python: [`src/erz_json_to_excel.py`](src/erz_json_to_excel.py);
- конфиг: блок `erz_json_to_excel` в `config.json`;
- пример: `input/erz_full_sample.json`;
- тесты: `src/Tests/test_erz_json_to_excel.py`.

**Листы:**
1. `_ERZ_GROUPS` — группа, регионы (`;\n`), компании группы, компании бренда;
2. `_ERZ_COMPANIES` — уникальный ИНН, тип (группа / бренд / оба), список групп,
   кол-во групп, «группа, регион», кол-во пар группа+регион.

**Шаги:**
1. `[v]` тестовый JSON
2. `[v]` скрипт + форматирование (автофильтр, freeze, wrap, ширины)
3. `[v]` config / README / ROADMAP / unit-тесты

### Этап 21 — разбор `_base_kg` → уникальные ИНН

**Цель:** из смарт-таблицы `_base_kg` извлечь пары «название компании ↔ ИНН» из колонки
«Компании группы (ЕРЗ)» и связать с «Наименование, регион» исходной строки;
собрать таблицу уникальных ИНН с агрегацией разных названий и регионов.

**Артефакты:**
- Python: `src/extract_base_kg_inn.py` (отдельный Excel на выходе);
- Power Query: `src/PowerQuery/_BASE_KG_INN.pq` (отдельный лист/запрос);
- конфиг: блок `base_kg_inn_extract` в `config.json`;
- пример: `input/base_kg_sample.xlsx`;
- тесты: `src/Tests/test_extract_base_kg_inn.py`.

**Шаги:**
1. `[v]` Раздел `base_kg_inn_extract` в `config.json` (входной файл, таблица, колонки, разделители, «ИНН», форматирование).
2. `[v]` Парсер фрагментов «Название (ИНН N)» с несколькими разделителями.
3. `[v]` Агрегация по уникальному ИНН: названия и «Наименование, регион» через `";\n"`.
4. `[v]` Python: запись Excel с жирными заголовками, автофильтром, freeze, ширинами.
5. `[v]` Power Query M-скрипт с той же логикой.
6. `[v]` Документация README/ROADMAP + unit-тесты.

### Этап 20 — предфильтр: все AND/OR-ключи

**Проблема:** предфильтр брал один «якорь» (сначала из AND, иначе из OR).  
Латинский токен (`Seven`) отбрасывал кириллический холдинг до полной проверки, хотя другие ключи (`СЕВЕН` / `САНС`) совпадали — ключи фактически исключались из участия.

**Исправление:**
- в предфильтр попадают **все** непустые `and_*` и `or_*` токены (`full` и `not`);
- кандидат, если найден **хотя бы один**; полная логика AND → OR → non без изменений;
- `*_non` участвуют только в полной проверке (исключения), не как положительный якорь;
- Python + `_HOLD_OD_GSZ_FAST` + `_HOLD_OD_GSZ_FAST_V2`.

## 1. Цель проекта

Автоматизировать привязку холдингов из `_HOLD_OD` к условным ГСЗ из `_base_gsz` в Excel 2016 без VBA — только Power Query (M).

**Результаты:**

1. Запрос `_HOLD_OD_GSZ` — `src/PowerQuery/_HOLD_OD_GSZ.pq`
2. Ускоренный запрос `_HOLD_OD_GSZ_FAST` — `src/PowerQuery/_HOLD_OD_GSZ_FAST.pq`
3. Генератор справочника ключей — `src/generate_gsz_keys.py`
4. Параллельный Python-матчер — `src/gsz_matcher_parallel.py`
5. Локальный Excel `output/_base_gsz_keys_YYYYMMDD_HHMMSS.xlsx` (не в git)

---

## 2. Исходные данные

### 2.1 `_HOLD_OD` (вход)

| Колонка | Роль |
|---------|------|
| `Холдинг` | Текст поиска |
| `ID холдинга` | Сохраняется |
| `Sum([ОД текущий год])` | Сохраняется |
| `Sum([ОД прошлый год])` | Сохраняется |

**Выход запроса добавляет:** `условное ГСЗ`, `Отладка_совпадения_ГСЗ`.

### 2.2 `_base_gsz` (справочник)

| Колонка | Роль |
|---------|------|
| `Наименование, регион` | Значение при совпадении |
| `key_and_full_1` … `key_and_full_3` | AND, подстрока |
| `key_or_full_1` … `key_or_full_3` | OR, подстрока |
| `key_and_not_1` … `key_and_not_3` | AND, отдельное слово |
| `key_or_not_1` … `key_or_not_3` | OR, отдельное слово |
| `key_fix_id` | Фиксированный ID холдинга (`"; "` — несколько); см. [ToDo/key_fix_id.md](ToDo/key_fix_id.md) |

Пустые ячейки ключей игнорируются. Строка без ключей не участвует в сопоставлении.

### 2.3 Источник имён для генератора ключей

Имена берутся из Excel-книги `gsz_matcher_parallel.input_xlsx` (сейчас `input/Tables+Holding.xlsx`):
смарт-таблица `_base_gsz`, колонка `gsz_column` («Наименование, регион»).
Файл `КейЛОАД.txt` больше не используется.

---

## 3. Бизнес-логика (согласовано)

### 3.1 Нормализация

`Text.Trim` + `Text.Lower` для «Холдинг» и всех ключей.

### 3.2 Режим `full`

Подстрока в любом месте текста.

### 3.3 Режим `not`

Отдельное слово: границы — начало/конец строки или не-буквенный символ. Буквы: кириллица + латиница.

### 3.4 AND

- Все непустые `key_and_full_*` и `key_and_not_*` обязательны.
- Позиции вхождений **не пересекаются** между всеми AND-полями строки.
- Если все AND пусты — блок считается пройденным.

### 3.5 OR

- Хотя бы одно непустое `key_or_full_*` или `key_or_not_*`.
- Если все OR пусты — блок не требуется.

### 3.6 AND + OR вместе

```
совпадение = AND_пройден И (OR_пуст ИЛИ OR_пройден)
```

### 3.7 Несколько совпадений

| Колонка | Правило |
|---------|---------|
| `условное ГСЗ` | Первое «Наименование, регион» по порядку в `_base_gsz` |
| `Отладка_совпадения_ГСЗ` | Все совпадения через `"; "` |
| Нет совпадений | `"-"` |

### 3.8 Прочее

- В ячейке ключа — одно слово.
- Новый запрос; `_HOLD_OD` не перезаписывается.
- Пустой «Холдинг» → `"-"`.

---

## 4. Архитектура Power Query

Файлы:
- `src/PowerQuery/_HOLD_OD_GSZ.pq` (базовая версия)
- `src/PowerQuery/_HOLD_OD_GSZ_FAST.pq` (ускоренная версия)

```
Источник_HOLD_OD ─────────────────────────────────────────┐
Источник_base_gsz → Справочник_буфер                      │
                                                          │
fnНормализоватьТекст                                      │
fnЭтоБуква                                                │
fnВсеПозицииFull / fnПозицииNot                           │
fnПроверитьAndТокены                                      │
fnПолеOrСовпало / fnПроверитьOrБлок                       │
fnСтрокаGszСовпала → fnНайтиВсеGsz                        │
                                                          ▼
                    СКолонкой_список_совпадений
                    СКолонкой_условное_ГСЗ
                    СКолонкой_отладка
                    Без_служебной_колонки → Результат
```

### Ограничения Excel 2016 (учтено)

| Проблема | Решение |
|----------|---------|
| `ExpandRecordColumn` 4 аргумента | Отдельные `Table.AddColumn` |
| `[_колонка]{0}` | `Record.Field` + `List.First` |
| `Table.AddColumn(..., type text)` | Без 4-го аргумента |
| `fnНайтиВсеGsz` 2 аргумента | Справочник в замыкании |
| `Table.Column` 3 аргумента | 2 аргумента + `List.Transform` |

---

## 5. Генератор ключей

Файл: `src/generate_gsz_keys.py`  
Конфиг: `config.json`

### Алгоритм

1. Чтение имён из `gsz_matcher_parallel.input_xlsx` → смарт-таблица `_base_gsz` → колонка `gsz_column`.
2. Короткое имя = часть до первой запятой.
3. Токенизация: пробелы, дефисы, скобки; стоп-формы (ООО, ГК…); отдельные буквы → `or_not` (+ транслит).
4. **Ключ только из своего имени** или транслит RU↔EN; чужие/выдуманные слова запрещены.
5. Подбор AND (`full`/`not`) по слотам из config; `*_non` автозаполнением не выдумываются.
6. Экспорт в `output/_base_gsz_keys_YYYYMMDD_HHMMSS.xlsx` (таймштамп при `output_add_timestamp=true`).

### Статистика последнего прогона

| Метрика | Значение |
|---------|----------|
| Источник | `input/Tables+Holding.xlsx` → `_base_gsz` |
| Строк в списке | 6939 |
| Уникальных ключей | ~1810 |
| С пометкой `ключи_задублированы` | ~5129 |
| Нарушений «ключ не из имени» | 0 |
| `*_non` авто | 0 |

Высокая доля дубликатов возможна из-за повторяющихся названий. Строки с `да` требуют ручной доработки.

---

## 6. Структура репозитория

| Путь | В git | Описание |
|------|-------|----------|
| `src/PowerQuery/_HOLD_OD_GSZ.pq` | да | M-скрипт |
| `src/generate_gsz_keys.py` | да | Генератор ключей (Excel → все key-колонки) |
| `src/generate_test_workbook.py` | да | Сборка тестовой книги `Tables+Holding.xlsx` |
| `src/gsz_matcher_parallel.py` | да | Параллельный Python-матчер |
| `src/Tests/test_gsz_matcher_parity.py` | да | Регресс: fast vs brute на всех холдингах |
| `config.json` | да | Параметры генератора и матчера |
| `input/Tables+Holding.xlsx` | **да** | Книга-источник (`_base_gsz` + `_HOLD_OD`) |
| `input/workbook.xlsx` | **да** | Старая тестовая книга (legacy) |
| `output/*.xlsx` | **нет** | Локальный результат (`output/.gitkeep` — в git) |
| прочие `*.xlsx` | **нет** | Генерируемые артефакты |

`.gitignore` исключает `output/*.xlsx`, `КейЛОАД.txt`, `LOGS/`, `log/`, `.venv/`.  
Исключения: `input/workbook.xlsx`, `input/Tables+Holding.xlsx`.

---

## 7. Тестовые сценарии

| # | Холдинг | Ключи | Ожидание |
|---|---------|-------|----------|
| T1 | `ГК ПИК` | and_full: `ПИК` | да |
| T2 | `ПИКНИК` | and_not: `ПИК` | нет |
| T3 | `ПИКНИК` | and_full: `ПИК` | да |
| T4 | `ПИГ ИГЛА` | and: `ПИГ`, `ИГЛА` | да |
| T5 | `ПИГЛА` | and: `ПИГ`, `ИГЛА` | нет |
| T6 | `пик строй` | and_full: `ПиК` | да |
| T7 | все ключи пусты | любой | нет |
| T8 | `А Б В` | and + or | по формуле п. 3.6 |

---

## 8. Производительность и оптимизация

Для рабочих книг с большим объемом данных (десятки тысяч строк `_HOLD_OD`) добавлен отдельный запрос `_HOLD_OD_GSZ_FAST` с сохранением бизнес-логики сопоставления:

1. Предподготовка строк `_base_gsz` (нормализация и токены) выполняется один раз.
2. Предфильтр кандидатов: **все** AND/OR-токены (`full`/`not`) строки; кандидат при любом совпадении.
3. Полная проверка AND/OR/non запускается только для кандидатов после предфильтра.
4. В `full`-поиске используется `Text.PositionOf` с `Occurrence.All`.

Ожидаемый эффект: существенное ускорение на больших массивах при сохранении функционального поведения запроса.
Базовый `_HOLD_OD_GSZ.pq` предфильтр не использует (полный перебор) — баг с одним якорем его не касался.

---

## 9. Python Roadmap (с учетом требований по окружению)

Цель: полностью перенести логику сопоставления из M в Python с возможностью масштабирования по CPU и гибкой настройкой таблиц/колонок.

### Этап 1. Паритет логики (завершено)
- реализованы правила `full/not`, `and/or`, непересечение AND-токенов;
- сохранено поведение выходных колонок (`условное ГСЗ`, `Отладка_совпадения_ГСЗ`).

### Этап 2. Конфигурируемость источников (завершено)
- поддержан выбор таблиц: `_HOLD_OD`, `_base_gsz` (или пользовательские имена);
- поддержан выбор колонок и наборов key-колонок через CLI;
- добавлена поддержка JSON-конфига аргументов.

### Этап 3. Оптимизация скорости (завершено)
- предобработка `_base_gsz` в мета-структуру один раз;
- предфильтр кандидатов по **всем** AND/OR-токенам (`full`/`not`), без отсечения до одного якоря;
- multiprocessing через `ProcessPoolExecutor`;
- индекс кандидатов по якорям (`not/full`) внутри воркеров;
- кэш позиций токенов на один холдинг для исключения повторных вычислений;
- **исправление not-якоря**: проверка через `positions_not`, а не `extract_words` (устранены ложные пропуски, напр. «мега» в «мега35»);
- **исправление предфильтра**: один латинский якорь больше не отбрасывает остальные ключи (кейс Seven/СЕВЕН + AND/OR);
- чтение Excel в `read_only` (двухпроходное);
- опциональное отключение форматирования всех ячеек (`format_data_vertical_center`);
- регресс-тест `src/Tests/test_gsz_matcher_parity.py`: 0 расхождений fast vs brute на 26 000 холдингов.
- добавлена обратная проекция на `_base_gsz`:
  - `найденный холдинг` (один матч: `[ID]: холдинг`, несколько: `есть пересечения по ключам`);
  - `Отладка_найденного_холдинга` (все найденные холдинги построчно).
- `output_format` расширен ключевой схемой конфигурации добавляемых колонок:
  - ключ типа данных (фиксированный) → `name` (изменяемый), `width`, `wrap`;
  - поддерживается обратная совместимость со старым списком по позиции.
- добавлены исключающие группы ключей:
  - `and_non`: строка исключается, только если найдены все non-ключи;
  - `or_non`: строка исключается, если найден хотя бы один non-ключ;
  - проверка `non` выполняется по названию холдинга без пробелов.
- добавлен Power Query `src/PowerQuery/_HOLD_OD_GSZ_FAST_V2.pq`:
  - поддержка `key_and_non_*` и `key_or_non_*`;
  - автоопределение колонок `key_<группа>_<N>` из заголовков `_base_gsz`;
  - ручной override через `Колонки_*_явно`.

### Этап 4. Промышленная калибровка (следующий)
- замер времени на реальной книге (44k × 7k);
- подбор `workers` и `work_batch_size` для конкретной машины;
- сверка результатов Python vs Power Query на контрольной выборке.
- рекомендуемый «живой» профиль логирования для длительных прогонов:
  - `work_batch_size = 20`
  - `progress_every_holdings = 500`
  - `progress_every_base_rows = 500`

### Бенчмарк (тестовая книга `input/workbook.xlsx`)

| Метрика | Значение |
|---------|----------|
| Холдингов | 26 000 |
| Строк `_base_gsz` | 6 921 |
| Workers | 8 |
| Время полного прогона | ~7–9 с (с вертикальным центрированием всех ячеек) |
| Parity fast vs brute | 0 расхождений |

### Ограничения по зависимостям
- используется только Python + пакет `openpyxl` из стандартной поставки Anaconda;
- внешние нестандартные библиотеки не требуются.

---

## 10. Следующие шаги

1. **`[ ]`** Прогон `_HOLD_OD_GSZ` на рабочей книге с реальными `_HOLD_OD` и сгенерированным `_base_gsz`.
2. **`[ ]`** Ручная доработка строк с `ключи_задублированы = да` в Excel.
3. **`[ ]`** При необходимости — донастройка стоп-слов и порогов в `config.json`.
4. **`[ ]`** Прогон `src/gsz_matcher_parallel.py` на боевом объёме и фиксация оптимальных `workers` / `work_batch_size`.
5. **`[ ]`** Сверка Python vs Power Query на контрольной выборке боевой книги.
6. **`[v]`** Реализация `key_fix_id` и колонки «статус» — см. [ToDo/key_fix_id.md](ToDo/key_fix_id.md).
7. **`[v]`** Предфильтр по всем AND/OR-токенам (без одного якоря) — Python + FAST/FAST_V2.
8. **`[v]`** Разбор `_base_kg` → уникальные ИНН (`extract_base_kg_inn.py`, `_BASE_KG_INN.pq`).
9. **`[v]`** Доп. колонки `_BASE_KG_INN`: кол-во регионов, наименование без города, freeze=1, ширины.

---

## 11. Ссылки

- [README.md](README.md) — инструкции пользователя
- [ToDo/Бизнес-требования_ГСЗ-сопоставление.md](ToDo/Бизнес-требования_ГСЗ-сопоставление.md) — полные требования
- [ToDo/key_fix_id.md](ToDo/key_fix_id.md) — план доработки: фиксированное соответствие по ID холдинга
- [ToDo.txt](ToDo.txt) — исходное ТЗ
- `src/extract_base_kg_inn.py` / `src/PowerQuery/_BASE_KG_INN.pq` — разбор ИНН из `_base_kg`
